# /to-prd project conventions

Populated at project init (via `/grill-with-docs` closing prompts), or auto-populated by `/to-prd` on first invocation if placeholders are still unfilled.

## PRD destination

`[Specify: 'tracker' OR 'local'. Default for solo continuous-deploy: 'local'.]`

## Tracker URL (only if destination = tracker)

`[Specify: e.g., https://github.com/<user>/<repo>/issues. Leave empty if local.]`

## Triage label (only if destination = tracker)

`[Specify: e.g., 'ready-for-agent'. Leave empty if local.]`
